public enum UnoColor { Red, Green, Blue, Yellow, Wild }
public enum UnoValue
{
    Zero, One, Two, Three, Four, Five, Six, Seven, Eight, Nine,
    Skip, Reverse, DrawTwo, Wild, WildDrawFour
}

public enum PlayerType { 
    Human, AI_RL, AI_Random 
}
